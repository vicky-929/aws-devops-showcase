name: Install-IIS
description: Installs the IIS web server role and management tools
schemaVersion: 1.0
phases:
  - name: build
    steps:
      - name: InstallIIS
        action: ExecutePowerShell
        inputs:
          commands:
            - Install-WindowsFeature -Name Web-Server -IncludeManagementTools
